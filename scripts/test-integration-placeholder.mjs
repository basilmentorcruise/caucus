#!/usr/bin/env node
// Placeholder for `pnpm test:integration`.
//
// The real cross-package integration harness is CAU-25 (NOT this ticket,
// CAU-1). Until then this script is an intentional no-op that exits 0 so the
// command exists and is wired into the workspace.
console.log(
  "[caucus] integration test harness not yet implemented (tracked by CAU-25); skipping. Exit 0.",
);
process.exit(0);
