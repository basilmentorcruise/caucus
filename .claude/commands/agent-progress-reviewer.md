---
description: Reviews app progress after an epic ships and re-plans the board. Use after an epic's release to correct course against the vision and open correction tickets.
---

Use the `progress-reviewer` subagent (its definition lives in `~/.claude/agents/progress-reviewer.md`). Read `CLAUDE.md` and
`docs/sdlc/GOVERNANCE.md` for project context first. Follow the agent's Required Output Format.

Task / focus: $ARGUMENTS
If no argument is given, act on the most relevant current work for this role.
