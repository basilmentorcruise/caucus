---
description: Specs a UI ticket before code, or runs the design-review gate after. Use for UI work needing HCI heuristics, WCAG 2.2 AA, and the token design system.
---

Use the `designer` subagent (its definition lives in `~/.claude/agents/designer.md`). Read `CLAUDE.md` and
`docs/sdlc/GOVERNANCE.md` for project context first. Follow the agent's Required Output Format.

Task / focus: $ARGUMENTS
If no argument is given, act on the most relevant current work for this role.
