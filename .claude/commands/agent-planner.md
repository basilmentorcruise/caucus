---
description: Captures or refines the product vision via interview and seeds CLAUDE.md. Use at kickoff, before a new epic, or to resolve a needs-attention product question.
---

Use the `planner` subagent (its definition lives in `~/.claude/agents/planner.md`). Read `CLAUDE.md` and
`docs/sdlc/GOVERNANCE.md` for project context first. Follow the agent's Required Output Format.

Task / focus: $ARGUMENTS
If no argument is given, act on the most relevant current work for this role.
