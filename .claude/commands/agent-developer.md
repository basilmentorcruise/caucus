---
description: Implements one well-specified ticket test-first in its own worktree and opens a draft PR. Use to build a ticket or to address gate feedback on one.
---

Use the `developer` subagent (its definition lives in `~/.claude/agents/developer.md`). Read `CLAUDE.md` and
`docs/sdlc/GOVERNANCE.md` for project context first. Follow the agent's Required Output Format.

Task / focus: $ARGUMENTS
If no argument is given, act on the most relevant current work for this role.
