{
  "name": "@caucus/integration",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-package integration-test harness: boots the backbone + ≥2 clients (CAU-25).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@caucus/backbone": "workspace:*",
    "@caucus/backbone-server": "workspace:*",
    "@caucus/hook": "workspace:*",
    "@caucus/mcp-server": "workspace:*",
    "@caucus/schema": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
