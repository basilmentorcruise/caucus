/**
 * Schema version. v0 is the only supported version (ratified and frozen in M0,
 * CAU-3). Breaking changes require bumping this and widening the version gate.
 */
export const SCHEMA_VERSION = 0 as const;
